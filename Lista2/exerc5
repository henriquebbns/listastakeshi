#include <iostream>
using namespace std;

int main()
{
    int a,b;

    cin>>a>>b;

    if(a==b)
        cout<<"Valores iguais";
    else if(a<b)
    {
        cout<<a<<" "<<b<<"\n";
        cout<<b<<" "<<a;
    }
    else
    {
        cout<<b<<" "<<a<<"\n";
        cout<<a<<" "<<b;
    }

    return 0;
}
